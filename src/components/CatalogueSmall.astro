---
import { getMedia } from "../services/wpService";

interface Props {
  title: string;
  description?: string;
  fileId: number;
  imageId: number;
}
const { title, description, fileId, imageId } = Astro.props;

const image: string = (await getMedia(imageId)).source_url;
const file: string = (await getMedia(fileId)).source_url;
---

<a
  href={file}
  title={title}
  target="_blank"
  class="card card-compact bg-neutral-content text-center w-full max-w-sm mx-auto hover:-translate-y-2 hover:shadow-2xl transition-all"
>
  <figure class="h-56">
    <img src={image} alt={title} class="w-full object-cover" />
  </figure>
  <div class="card-body">
    <h2 class="card-title justify-center">{title}</h2>
    {description && <p>{description}</p>}
  </div>
</a>
